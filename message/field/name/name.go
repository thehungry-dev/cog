// Package name provides constants used for field names that have special meaning, such as identifying the body of the message
package name

const NamePrefix = "__log__"

const (
	Body = NamePrefix + "body"
)
